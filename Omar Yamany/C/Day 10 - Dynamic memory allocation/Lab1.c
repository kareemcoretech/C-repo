#include<stdio.h>
#include<stdlib.h>
#include"types.h"

void main()
{
	u8 size;
	printf("Enter number of elements: ");
	scanf("%d", size);
	u8 *ptr = (u8*)calloc(10,sizeof(u8));
	
}

void sort(u8 *ptr, size)
{
	
}